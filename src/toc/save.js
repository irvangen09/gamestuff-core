/**
 * GameStuff TOC is a dynamic block; the markup is entirely generated
 * by render.php when the frontend renders, so save() doesn't store
 * any markup.
 *
 * @return null
 */
export default function save() {
	return null;
}
